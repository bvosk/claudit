services:
  mitmproxy-capture:
    build: .
    container_name: mitmproxy-capture
    ports:
      - "8080:8080"
    volumes:
      - ./captures:/app/captures
      - ./prompts:/app/prompts
    env_file:
      - path: .env
        required: false
