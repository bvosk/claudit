services:
  mitmproxy-capture:
    build: .
    container_name: mitmproxy-capture
    environment:
      - PROXY_PORT=${PROXY_PORT:-8080}
    ports:
      - "${PROXY_PORT:-8080}:${PROXY_PORT:-8080}"
    env_file:
      - .env
